import 'package:flutter/material.dart';

class ShowAllComment extends StatelessWidget {
  const ShowAllComment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
